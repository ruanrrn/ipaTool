# pnpm 配置文件
# 允许构建脚本运行
enable-pre-post-scripts=true

# 严格对等依赖
strict-peer-dependencies=false

# 自动安装对等依赖
auto-install-peers=true

# 忽略构建脚本
# ignore-scripts=false

# 使用 Node.js 原生模块
# better-sqlite3 需要原生构建
shamefully-hoist=true

# 锁文件格式
lockfile-format=v6

# 优先使用 npm 镜像（如果需要）
# registry=https://registry.npmmirror.com
